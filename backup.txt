using(var reader=new FileStream("hello.xlsx",FileMode.Open))
            {
                byte[] bt=new byte[reader.Length];
                reader.Read(bt,0,bt.Length);
                var file=Convert.ToBase64String(bt);
                reader.Close();
                var excel=new StreamReader("test.txt");
                file=excel.ReadToEnd();
                System.IO.File.WriteAllBytes("out.xlsx",Convert.FromBase64String(file));
            }